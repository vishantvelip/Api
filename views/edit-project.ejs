

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Edit Project</title>
  <style>
    body {
      margin: 0;
      font-family: 'Segoe UI', Arial, sans-serif;
      background: linear-gradient(135deg, #29323c 70%, #485563 100%);
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
    }
    .container {
      background: rgba(255,255,255,0.12);
      border-radius: 20px;
      box-shadow: 0 8px 32px rgba(0,0,0,0.23);
      width: 99vw;
      max-width: 430px;
      margin: 40px auto;
      padding: 36px 24px 30px 24px;
      display: flex;
      flex-direction: column;
      align-items: center;
      backdrop-filter: blur(2px);
      border: 1px solid rgba(255,255,255,0.13);
    }
    h2 {
      color: #fff;
      font-size: 2.1rem;
      text-align: center;
      margin-bottom: 26px;
      letter-spacing: 1.3px;
      font-weight: 700;
      text-shadow: 0 2px 10px #2222;
    }
    .form-group {
      width: 100%;
      margin-bottom: 15px;
      display: flex;
      flex-direction: column;
      gap: 2px;
    }
    .form-group label {
      color: #e4e4e4;
      font-size: 1.02rem;
      margin-bottom: 2px;
      font-weight: 500;
      letter-spacing: 0.3px;
    }
    .form-group input,
    .form-group textarea {
      width: 100%;
      padding: 11px;
      border-radius: 7px;
      border: 1px solid #e9ecef;
      background: #f8fafb;
      font-size: 1.06rem;
      margin-top: 2px;
      box-sizing: border-box;
      outline: none;
      transition: box-shadow 0.23s, border 0.21s;
    }
    .form-group input:focus, .form-group textarea:focus {
      box-shadow: 0 0 0 2px #5bc6ff44;
      background: #fff;
      border: 1.5px solid #5bc6ff;
    }
    .form-group textarea {
      resize: vertical;
      min-height: 80px;
      font-family: inherit;
    }
    .form-group input[type="file"] {
      padding: 5px 2px;
      background: #fff;
      border: none;
      font-size: 1rem;
    }
    .form-group img {
      max-width: 130px;
      height: auto;
      margin-top: 10px;
      border-radius: 5px;
      background: #eee;
      box-shadow: 0 2px 8px #0001;
      display: block;
    }
    .form-group p {
      color: #aeb0b3;
      font-size: 0.94rem;
      margin: 0;
      margin-top: 5px;
    }
    .message {
      width: 100%;
      margin-bottom: 20px;
      padding: 12px 10px;
      border-radius: 7px;
      text-align: center;
      font-size: 1.04rem;
      background: linear-gradient(93deg, #ff6a6a, #ffb86c);
      color: #fff;
      box-shadow: 0 2px 8px rgba(220,53,69,0.09);
      animation: fadeIn 0.7s;
      font-weight: 600;
      letter-spacing: 0.2px;
    }
    .message.success {
      background: linear-gradient(to right, #38c172, #48e6a3);
      color: #fff;
    }
    .actions {
      width: 100%;
      display: flex;
      justify-content: space-between;
      gap: 10px;
      margin-top: 12px;
    }
    button[type="submit"], .back-btn {
      background: linear-gradient(90deg, #00c6ff, #0072ff);
      color: #fff;
      padding: 12px 0;
      border: none;
      border-radius: 7px;
      cursor: pointer;
      font-size: 1.07rem;
      font-weight: 600;
      letter-spacing: 0.5px;
      transition: background 0.19s, transform 0.09s;
      text-decoration: none;
      text-align: center;
      box-shadow: 0 2px 8px #0072ff27;
      width: 49%;
      margin-top: 0;
      min-width: 120px;
    }
    button[type="submit"]:hover, .back-btn:hover {
      background: linear-gradient(90deg, #0056b3, #00c6ff);
      transform: translateY(-1.2px) scale(1.035);
    }
    .back-btn {
      background: linear-gradient(90deg,#43e97b,#38f9d7);
      font-weight: 700;
      display: inline-block;
      text-align: center;
      line-height: 1.3;
    }
    .back-btn:hover {
      background: linear-gradient(90deg,#218838,#43e97b);
      color: #fff;
    }
    @media (max-width: 600px) {
      .container { padding: 15px 2vw; max-width: 99vw;}
      .actions { flex-direction: column; gap: 10px;}
      button[type="submit"], .back-btn { width: 100%; }
    }
    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(16px);}
      to { opacity: 1; transform: translateY(0);}
    }
  </style>
</head>
<body>
  <div class="container">
    <h2>Edit Project</h2>
    <% if (message) { %>
      <div class="message <%= message.includes('Failed') ? '' : 'success' %>">
        <%= message %>
      </div>
    <% } %>
    <% if (project) { %>
      <form action="/api/update-project/<%= project._id %>" method="post" enctype="multipart/form-data" autocomplete="off">
        <div class="form-group">
          <label for="name">Project Name</label>
          <input type="text" id="name" name="name" value="<%= project.name %>" required autocomplete="off" maxlength="100">
        </div>
        <div class="form-group">
          <label for="title">Project Title</label>
          <input type="text" id="title" name="title" value="<%= project.title %>" required autocomplete="off" maxlength="120">
        </div>
        <div class="form-group">
          <label for="description">Project Description</label>
          <textarea id="description" name="description" rows="5" required maxlength="900"><%= project.description %></textarea>
        </div>
        <div class="form-group">
          <label for="projectImg">Project Image</label>
          <input type="file" id="projectImg" name="projectImg" accept="image/jpeg,image/png">
          <% if (project.projectImg) { %>
            <p>Current image:</p>
            <img src="/<%= project.projectImg %>" alt="Current project image">
          <% } %>
        </div>
        <div class="actions">
          <button type="submit">Update Project</button>
          <a href="/api/view" class="back-btn">View Projects</a>
        </div>
      </form>
    <% } else { %>
      <div class="message">Project not found.<br>
        <a href="/api/view" class="back-btn" style="display:inline-block;margin-top:10px;">Return to projects</a>
      </div>
    <% } %>
  </div>
  <script>
    document.querySelector('form')?.addEventListener('submit', (e) => {
      const name = document.getElementById('name')?.value.trim();
      const title = document.getElementById('title')?.value.trim();
      const description = document.getElementById('description')?.value.trim();
      if (!name || !title || !description) {
        e.preventDefault();
        alert('All fields must be filled.');
      }
    });
  </script>
</body>
</html>
